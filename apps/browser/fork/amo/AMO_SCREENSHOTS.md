# Screenshot status

This workflow requests Mozilla signing through the unlisted self-distribution channel. It does not
create a public AMO listing, so listing screenshots are not submitted.

Before any future public listing, capture current Firefox screenshots of:

1. the self-hosted server selection screen;
2. a vault containing only synthetic test records;
3. the autofill menu on a synthetic test page;
4. the settings About screen and independent product identity.

Remove real URLs, accounts, credentials, vault data, browser history, and personal information from
every screenshot. Captions must describe only behavior visible in the submitted version.
